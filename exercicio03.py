base_retangulo = int(input('base do retagulo'))

altura_retangulo = int(input('base da altura'))

area = base_retangulo * altura_retangulo

print(f"a area do retangulo é:{area}")

perimetro = 2 * (base_retangulo + base_retangulo)

print(f" o valor do perimetro:{perimetro}")

diagonal = (base_retangulo**2 + altura_retangulo**2)**0.5

print(f"a area do diagonal:{diagonal} ")